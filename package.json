{
  "name": "@markusylisiurunen/tau",
  "version": "0.2.116",
  "type": "module",
  "bin": {
    "tau": "./dist/main.js"
  },
  "files": [
    "dist/"
  ],
  "exports": {
    "./sdk": {
      "types": "./dist/sdk/index.d.ts",
      "default": "./dist/sdk/index.js"
    }
  },
  "scripts": {
    "build": "(cd src/diff_tool/app && npm run build) && tsc -p tsconfig.json",
    "postbuild": "node scripts/copy-starter.js && node scripts/copy-static-prompts.js && node scripts/copy-diff-tool-app.js && find dist -name '*.d.ts' ! -path 'dist/sdk/*' -delete",
    "check": "prettier --log-level warn -w .tau/**/*.md docs/**/*.md src/core/static/**/*.md starter/**/*.md *.md && biome check --write . && tsc -p tsconfig.json --noEmit && (cd src/diff_tool/app && npm run check)",
    "precheck": "node scripts/generate-version.js && node scripts/generate-themes.js",
    "prebuild": "node scripts/generate-version.js && node scripts/generate-themes.js",
    "start": "node dist/main.js",
    "test": "npm run build && vitest run"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.75.4",
    "@earendil-works/pi-tui": "^0.75.4",
    "chalk": "^5.6.2",
    "file-type": "^21.3.4",
    "sharp": "^0.34.5",
    "strip-ansi": "^7.2.0",
    "typebox": "^1.1.38",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/node": "^25.9.1",
    "colorjs.io": "^0.6.1",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
